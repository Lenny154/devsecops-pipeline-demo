{
  "name": "devsecops-pipeline-demo",
  "version": "1.0.0",
  "description": "Phase 3 Project 1 — DevSecOps pipeline with security scanning",
  "main": "src/index.js",
  "scripts": {
    "start": "node src/index.js",
    "test": "jest --coverage",
    "lint": "eslint src/ --ext .js",
    "lint:security": "eslint src/ --ext .js --rulesdir .eslint-security"
  },
  "dependencies": {
    "express": "^4.18.2",
    "prom-client": "^15.1.0"
  },
  "devDependencies": {
    "jest": "^29.7.0",
    "supertest": "^6.3.4",
    "eslint": "^8.57.0",
    "eslint-plugin-security": "^2.1.1"
  },
  "jest": {
    "testEnvironment": "node",
    "coverageThreshold": {
      "global": {
        "lines": 80
      }
    }
  }
}
